[pytest]
# Prevent pytest from collecting duplicate test copies under generated output directories.
norecursedirs =
    runs
    target
    .git
    .venv
    venv
    __pycache__
    *.egg-info
addopts = -q
