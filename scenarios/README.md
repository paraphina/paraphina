# Scenario Registry

Scenarios are versioned YAML specs used by `sim_eval` runner.

- v1: minimal synthetic + historical-stub support

## CI smoke suite (recommended)
- scenarios/v1/synth_baseline.yaml
- scenarios/v1/synth_jump.yaml

## Research suite (expand later)
- add q0 sweep scenarios
- add latency spike scenarios
- add fee sign flip scenarios
