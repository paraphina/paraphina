suite_id: ci_smoke_v1
suite_version: 1

# If you want the runner to do determinism-by-repetition, we’ll implement it.
repeat_runs: 2

# Where to write run artifacts (runner will create per-scenario/seed subdirs).
out_dir: runs/ci

# Scenario list is just file paths into the committed registry.
scenarios:
  - path: scenarios/v1/synth_baseline.yaml
  - path: scenarios/v1/synth_jump.yaml
  - path: scenarios/v1/historical_replay_stub.yaml
